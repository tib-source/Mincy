'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  /* Put your mantine theme override here */
  colors: { "dark":['#f5f5f5', '#e0e0e0', '#c2c2c2', '#9e9e9e', '#424242', '#616161', '#424242', '#212121', '#1a1a1a', '#0d0d0d'],}
});
