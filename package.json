{
	"name": "mincy",
	"private": true,
	"workspaces": [
		"client",
		"server",
		"agent",
		"shared"
	],
	"scripts": {
		"dev": "bunx turbo run dev",
		"build": "bunx turbo run build",
		"test": "bunx turbo run test",
		"types": "bun --cwd shared run build",
		"lint": "bunx biome lint --write",
		"format": "bunx biome format --write",
		"gen:types": "bunx supabase gen types typescript --schema public --project-id xguaudrvdjmxteufzxvt > shared/src/database.types.ts"

	},
	"dependencies": {
		"@types/dockerode": "^3.3.44",
		"dockerode": "^4.0.9",
		"pino": "^10.1.0",
		"pino-pretty": "^13.1.2",
		"supabase": ">=1.8.1",
		"zod": "^4.1.12",
		"zustand": "^5.0.10"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.13",
		"@types/bun": "latest",
		"turbo": "^2.5.8"
	},
	"packageManager": "bun@1.3.0"
}
